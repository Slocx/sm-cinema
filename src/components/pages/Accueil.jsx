import React, { Fragment } from "react";

export const Accueil = (props) => {
    return(
        <Fragment>
            <h1> Page Accueil</h1>
            <div>
                Ceci est la page Accueil
            </div>
        </Fragment>
    )
};

export default Accueil;